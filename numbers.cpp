// on p. 64 
// https://git-scm.com/docs/git-credential-store
// https://www.tecmint.com/fix-git-user-credentials-for-https/

// basic start 
#include "std_lib_facilities.h"

int main()
{
    cout<<"Please enter an int value";
    int a; 
    cin >> a; 
    double n = a; 
    cout << "n ==" << a
            <<"\nn+1 == " << n+1
            << "\nthree times n == " << 3*n 
            << "\nntwice n == " << n+n
            << "\nn squared == " << n*n 
            << "\nhalf of n == " << n/2
            << "\nsquare root of n ==" << sqrt(n)
            << endl; // another name for endline
}
